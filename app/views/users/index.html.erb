<div id="users" class="index-items">
  <% @users.each do |user| %>
    <div class="index-item">
      <%= render user %>
      <p>
        <%= link_to t("views.common.show", name: User.model_name.human), user_path(user) %>
      </p>
    </div>
  <% end %>
</div>

<%= paginate @users %>
